<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="AppTheme.Base">

    </style>

    <style name="AppTheme.Base" parent="Theme.AppCompat.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="EditTextRegister" parent="TextAppearance.AppCompat.Large">
        <item name="android:paddingTop">@dimen/editTextPaddingHeight</item>
        <item name="android:paddingBottom">@dimen/editTextPaddingHeight</item>
        <item name="android:paddingLeft">@dimen/editTextPaddingWidth</item>
        <item name="android:paddingRight">@dimen/editTextPaddingWidth</item>
        <item name="android:layout_margin">@dimen/editTextMargin</item>
        <item name="android:background">@drawable/bg_edit_text</item>
    </style>

    <style name="AppTheme.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />


</resources>
